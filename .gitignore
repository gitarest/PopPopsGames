# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Saved player scores and event log (runtime data — never commit)
scores.json
scores.json.migrated
events.log

# SQLite database (runtime data — never commit)
game.db

# Editor / OS
.vscode/*.log
.DS_Store
Thumbs.db
static/*.tmp.*

# Deploy credentials (never commit)
.claude/skills/deploy/server.env
